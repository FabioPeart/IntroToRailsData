<h2>Users Found: <%= @users.size %></h2>

<% if @users.empty? %>
    <p>Sorry, no user found.</p>
<% else %>
    <ul>
        <% @users.each do |user| %>
        <li><%= link_to user.name, user %> </li>
        <% end %>
    </ul>
<% end %>

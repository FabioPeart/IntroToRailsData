<h1>Users</h1>

<%= form_tag(results_search_index_path, method: "get") do %>
  <%= label_tag(:q, "Search for:") %>
  <%= text_field_tag(:q) %>
  <%= submit_tag("Search") %>
<% end %>

<ul>
    <% @users.each do |user| %>
    <li><%= link_to user.name, user %></li>
    <% end %>
</ul>
